language: ruby
rvm:
 - "2.2.2"
# uncomment this line if your project needs to run something other than `rake`:
script: bundle exec rspec 
services:
  - mongodb
  
before_script:
- mongo gh-to-pivotal-tracker-test --eval 'db.addUser("travis", "test");'